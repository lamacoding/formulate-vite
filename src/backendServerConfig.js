const serverAddress = "http://localhost";
const serverPort = 8080;

const aiServerAddress = "https://y3hqfr.myvserver.online"
const aiPort = 8443;

export const serverUri = serverAddress + ":" + serverPort;
export const aiServerUri = aiServerAddress + ":" + aiPort;